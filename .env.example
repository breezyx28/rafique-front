# API base URL (development). Production uses .env.production
VITE_API_URL=http://localhost:3001/api

# SCP deploy (optional – or set env vars before npm run deploy)
# SSH key auth only; no password.
SSH_HOST=kat.sd
SSH_USER=kattop
# SSH_KEY=/path/to/private/key
REMOTE_DIR=public_html/rafique/frontend
